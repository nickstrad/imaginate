See [AGENTS.md](./AGENTS.md). All instructions there apply to Claude.

In particular: when invoked via `/plan`, `/simplify`, `/review`, `/security-review`, or `/drift-detection`, load the docs listed under that command in `AGENTS.md` **before** doing the work.
