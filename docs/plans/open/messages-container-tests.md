# messages-container.tsx test harness

- ⬜ `messages-container.tsx` (`src/modules/projects/ui/components/messages-container.tsx`) — needs a React Testing Library + TRPC test harness; track separately. No test file exists yet.
